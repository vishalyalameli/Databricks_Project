# The main pipeline for Databricks_Project_Bundle

resources:
  pipelines:
    Databricks_Project_Bundle_etl:
      name: Databricks_Project_Bundle_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      root_path: "../src/Databricks_Project_Bundle_etl"

      libraries:
        - glob:
            include: ../src/Databricks_Project_Bundle_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
