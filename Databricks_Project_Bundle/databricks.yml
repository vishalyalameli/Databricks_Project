# This is a Databricks asset bundle definition for Databricks_Project_Bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: Databricks_Project_Bundle
  uuid: a89620a0-f15b-4dba-a5f6-c98ee4b40c6b

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-632777ba-c84d.cloud.databricks.com
    variables:
      catalog: data_engineering
      schema: ${workspace.current_user.short_name}
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://dbc-632777ba-c84d.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/yalamelivishal555@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/yalamelivishal555@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: data_engineering
      schema: prod
    permissions:
      - user_name: yalamelivishal555@gmail.com
        level: CAN_MANAGE
